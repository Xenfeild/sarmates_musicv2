
<div class="card card-compact w-96 bg-base-100 shadow-xl">
  <figure><img src="" alt="Shoes" />{{ new.urlimg }}</figure>
  <div class="card-body">
    <h2 class="card-title">{{ new.title }}</h2>
    <p>{{ new.content }}</p>
    <div class="card-actions justify-end">
      <a href="{{ path('app_show', { 'id':new.id }) }}" class="btn btn-primary">Lire</a>
    </div>
  </div>
</div>